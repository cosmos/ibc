# sample config file
server:
  listenAddr: 0.0.0.0:3000

db:
  type: sqlite
  url: ibc.db
  # type: postgres
  # url: postgres://user:password@host:port/dbname

# chains declares how to reach each chain; referenced by other blocks via chainId.
# connections are relayed bi-directionally, so both chains configure a client.
# chains:
#   - chainId: "1"
#     evm:
#       rpc: https://ethereum-rpc.example.com
#   - chainId: "8453"
#     evm:
#       rpc: https://base-rpc.example.com
#
# relayer:
#   chains:
#     - chainId: "1"
#       evm:
#         contracts:
#           ics26Router: "0x..."
#       clients:
#         - id: "base-0"
#           type: "attestation"
#           counterpartyChainId: "8453"
#     - chainId: "8453"
#       evm:
#         contracts:
#           ics26Router: "0x..."
#       clients:
#         - id: "ethereum-0"
#           type: "attestation"
#           counterpartyChainId: "1"
