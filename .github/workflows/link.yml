name: Link and E2E

on:
  push:
    branches: [main, development]
    paths:
      - "link/**"
      - "e2e/**"
      - "Makefile"
      - ".github/workflows/link.yml"
  pull_request:
    branches: [main, development]
    paths:
      - "link/**"
      - "e2e/**"
      - "Makefile"
      - ".github/workflows/link.yml"

jobs:
  lint:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        module: [link, e2e, e2e/internal/harness]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version-file: ${{ matrix.module }}/go.mod
          cache-dependency-path: ${{ matrix.module }}/go.sum
      - uses: golangci/golangci-lint-action@v8
        with:
          version: v2.12.2
          working-directory: ${{ matrix.module }}

  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        include:
          - module: link
            packages: ./...
          - module: e2e
            packages: ./e2etest ./internal/... ./stub/...
          - module: e2e/internal/harness
            packages: ./...
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version-file: ${{ matrix.module }}/go.mod
          cache-dependency-path: ${{ matrix.module }}/go.sum
      - if: matrix.module == 'e2e/internal/harness'
        run: make build-link
      - if: matrix.module == 'e2e/internal/harness'
        run: docker info
      - if: matrix.module == 'e2e/internal/harness'
        run: go test -count=1 ${{ matrix.packages }}
        working-directory: ${{ matrix.module }}
        env:
          IBC_BIN: ${{ github.workspace }}/link/bin/ibc
      - if: matrix.module != 'e2e/internal/harness'
        run: go test ${{ matrix.packages }}
        working-directory: ${{ matrix.module }}

  e2e:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version-file: e2e/go.mod
          cache-dependency-path: |
            link/go.sum
            e2e/go.sum
            e2e/internal/harness/go.sum
      - run: docker info
      - run: make test-e2e
      - run: make test-e2e E2E_PKGS=./negative
