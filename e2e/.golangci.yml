# Lint config for the e2e module. It mirrors link/.golangci.yml except that it
# includes tests and disables revive's exported rule: this is test infrastructure,
# not a public library surface.
version: "2"

run:
  go: "1.26"
  timeout: 30m

issues:
  max-same-issues: 50

formatters:
  enable: [gci, gofmt, gofumpt, golines]
  exclusions:
    generated: lax
  settings:
    gci:
      sections:
        - "standard"
        - "prefix(github.com/cosmos/ibc)"
        - "default"
        - "alias"
        - "blank" # e.g. sql drivers
    gofmt:
      rewrite-rules:
        - pattern: "interface{}"
          replacement: "any"
    golines:
      max-len: 120

linters:
  enable:
    - asciicheck
    - bodyclose
    - copyloopvar
    - dogsled
    - dupl
    - goconst
    - govet
    - gocritic
    - misspell
    - nakedret
    - nolintlint
    - staticcheck
    - unconvert
    - unused
    - revive
    - errcheck
    - errorlint

  settings:
    dogsled:
      max-blank-identifiers: 3
    misspell:
      locale: US
    govet:
      enable: [shadow]
    revive:
      enable-default-rules: true
      rules:
        - name: package-comments
          disabled: true
        - name: exported
          disabled: true
  exclusions:
    generated: lax
    rules:
      - path: (.+)_test\.go
        linters: [goconst]
